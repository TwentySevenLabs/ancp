using DoesNotExist

function main()
    count::Int = "bad"
    println(missing_symbol)
    takes_two(1)
    if true
        println("missing end for function and if")

function takes_two(a, b)
    a + b
end

function definitely_broken(
